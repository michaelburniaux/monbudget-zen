workflows:
  monbudget-zen:
    name: Mon Budget ZEN
    max_build_duration: 60
    scripts:
      - name: Verify application files
        script: |
          echo "Mon Budget ZEN"
          ls -la
    artifacts:
      - "**/*"
